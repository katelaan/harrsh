# Doubly-linked null-terminated trees
# Every node points to left child, right child and parent,
# where the latter is given as 2nd parameter
dltree <= x1 -> (nil, nil, x2) ;
dltree <= x1 -> (y, z, x2) * dltree(y,x1) * dltree(z,x1)
